####### This block can be deleted from resulting .env.deployment #######
# If building off the Github repo for the first time, You should copy 
# this file to env.deployment. It is gitignore'd. Leave this one as 
# is for others to refer to when they do the same.
####### This block can be deleted from resulting .env.deployment #######

# Deployment Configuration - DO NOT COMMIT TO GIT
# This file contains sensitive information for AWS deployment

# OpenAI API Key - Used by Lambda function for tag generation
OPENAI_API_KEY=<your-openai-api-key-here>

# Google OAuth Credentials - Used for social login
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Apple Sign In Configuration - Used for Apple OAuth
# Services ID you created in Apple Developer Portal
APPLE_SERVICES_ID=com.anystupididea.quoteme.signin

# Your Apple Developer Team ID (found in Apple Developer → Account → Membership)
APPLE_TEAM_ID=YOUR_TEAM_ID

# Key ID from your Apple Sign In key (created in Apple Developer → Keys)  
APPLE_KEY_ID=YOUR_KEY_ID

# Private key content from your .p8 file (include the full content with BEGIN/END lines)
APPLE_PRIVATE_KEY='-----BEGIN PRIVATE KEY-----
MIGTAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBHkwdwIBAQQgYOUR8LG...
-----END PRIVATE KEY-----'

# Certificate ARN for custom domain (if needed)
CERTIFICATE_ARN=if-needed-put-it-here

# Instructions:
# 1. Keep this file LOCAL ONLY - never commit to git
# 2. Use for SAM deployments with the automated ./deploy.sh script
#    OR manually with:
#    source .env.deployment
#    sam deploy --parameter-overrides OpenAIApiKey="$OPENAI_API_KEY" GoogleClientId="$GOOGLE_CLIENT_ID" GoogleClientSecret="$GOOGLE_CLIENT_SECRET" AppleServicesId="$APPLE_SERVICES_ID" AppleTeamId="$APPLE_TEAM_ID" AppleKeyId="$APPLE_KEY_ID" ApplePrivateKey="$APPLE_PRIVATE_KEY"
# 3. Store a backup copy in a secure password manager
# 4. Get Apple credentials from:
#    - Team ID: Apple Developer → Account → Membership
#    - Services ID: Create in Apple Developer → Identifiers
#    - Key ID & Private Key: Create in Apple Developer → Keys → "Sign in with Apple"